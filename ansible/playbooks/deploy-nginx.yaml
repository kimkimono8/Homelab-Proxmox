---
- name: Deploy and Configure Ingress NGINX Router
  hosts: nginx
  become: true
  vars:
    nginx_virtual_hosts:
      - domain: agh.home
        upstream: http://192.168.1.21:8083
      - domain: hass.home
        upstream: http://192.168.1.24:8123
  roles:
    - nginx
