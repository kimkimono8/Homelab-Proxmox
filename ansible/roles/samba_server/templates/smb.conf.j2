Warning: Identity file /root/.ssh/id_rsa not accessible: No such file or directory.
[global]
   workgroup = WORKGROUP
   server string = Arch-Server Samba
   server role = standalone server
   log file = /var/log/samba/%m.log
   max log size = 50
   dns proxy = no

   # Modern SMB & Encryption
   server min protocol = SMB2_02
   client min protocol = SMB2
   server smb encrypt = default

   # macOS & iOS Optimization (Apple VFS Fruit)
   vfs objects = catia fruit streams_xattr
   fruit:metadata = stream
   fruit:model = Macmini
   fruit:posix_rename = yes
   fruit:veto_appledouble = no
   fruit:wipe_intentionally_left_blank_rfork = yes
   fruit:delete_empty_adfiles = yes

[Downloads]
   comment = Torrent Downloads
   path = /mnt/hdd2/downloads
   browseable = yes
   read only = no
   guest ok = no
   create mask = 0664
   directory mask = 0775
   valid users = {{ samba_user }}

[Downloads]
   comment = Torrent Downloads
   path = /mnt/hdd2/downloads
   browseable = yes
   read only = no
   guest ok = no
   create mask = 0664
   directory mask = 0775
   valid users = {{ samba_user }}

[RootFS]
   comment = Entire Root Filesystem
   path = /
   browseable = yes
   read only = no
   guest ok = no
   create mask = 0775
   directory mask = 0775
   valid users = {{ samba_user }}
